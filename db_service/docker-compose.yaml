services:
  web:
    container_name: db_service
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    environment:
      - ENV=dev
    restart: always
    networks:
      - rec-sys-network
networks:
  rec-sys-network:
    external: true
    name: rec-sys-network
    driver: bridge