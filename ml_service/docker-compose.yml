services:
  web:
    container_name: ml_service
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - ENV=production
    restart: always
    networks:
      - rec-sys-network
networks:
  rec-sys-network:
    external: true
    name: rec-sys-network
    driver: bridge