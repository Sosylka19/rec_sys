stages:
  - deploy

deploy:
  stage: deploy
  tags:
    - shell
  script:
    - cd db_service
    - echo "Deplying app to remote server..."
    - docker compose up -d --build
  rules:
    - changes:
      - db_service/**/*