stages:
  - test
  - deploy
test_job:
  stage: test
  tags:
    - shell
  script:
    - echo "Hello from GitLab Runner!"
    - pwd
    - ls -la

deploy:
  stage: deploy
  tags:
    - shell
  script:
    - echo "Deploying app to remote server."
    - docker compose up -d --build

